{
  "name": "missao-while-server",
  "version": "1.0.0",
  "description": "Servidor multiplayer para o jogo Loop Infinito - Missão While",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p public && cp index.html regras.html public/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
